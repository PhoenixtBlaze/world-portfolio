{
  "name": "world-portfolio",
  "private": true,
  "version": "0.1.0",
  "description": "World Portfolio — tile-based procedural city built with FlameCore",
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/ensure-flamecore.mjs",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:pages": "node scripts/verify-repo-contents.mjs && tsc --noEmit && vite build && node scripts/prune-pages-dist.mjs && node scripts/verify-pages-dist.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.13.0",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.3.0",
    "vite": "^8.0.14"
  }
}
